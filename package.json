{
    "name": "nexusdash-root",
    "version": "1.0.0",
    "description": "Root package for deployment",
    "main": "server/index.js",
    "scripts": {
        "start": "cd data_engine && uvicorn main:app --host 0.0.0.0 --port 8000 & node ../server/index.js",
        "build": "npm install && cd client && npm install && npm run build"
    },
    "keywords": [],
    "author": "",
    "license": "ISC",
    "dependencies": {
        "axios": "^1.13.2",
        "bcryptjs": "^2.4.3",
        "body-parser": "^2.2.2",
        "cors": "^2.8.5",
        "dotenv": "^17.2.3",
        "express": "^5.2.1",
        "jsonwebtoken": "^9.0.2",
        "multer": "^2.0.2"
    },
    "engines": {
        "node": ">=16.0.0"
    }
}